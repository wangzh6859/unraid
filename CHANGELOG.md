# 更新日志

> Release 的更新说明读取本文件内容（每次打包前记得把最新改动写到这里）。

## 最新版（1.0.x）

### 修复

- 修复 GPU 查询返回 null 导致首页系统信息（主机名 / CPU 型号 / 温度 / 频率）全部丢失的问题，GPU 改为独立查询，不再连累其他数据
- 修复磁盘 SMART 属性无法获取：改用正确的 `/Dashboard` 页面路径、阵列磁盘名与 SMART 页签参数
- 修复实时网速始终显示 0：仪表盘速率单位是 Kbps / Mbps（比特每秒），已按字节每秒正确换算
- 修复浅色主题下提示文字显示为白字、看不清的问题
- 修复文件管理页"去设置"按钮文字不居中

### 新增

- 实时网络上传 / 下载速率：7.2.x 无需升级系统，通过 webGUI 会话抓取仪表盘数据频道实现
- 处理器 / GPU / 磁盘温度传感器（lm-sensors / smartctl / IPMI）
- 磁盘 SMART 完整属性表（值 / 最差 / 阈值 / 原始值，含重映射扇区、通电时长等）
- 整机重启 / 关机按钮（首页右上角，10 秒倒计时确认防误触）
- 设置页集中管理：多地址自动选择、WebDAV（地址自动补 `/dav`）、系统登录（root 密码）、主题切换（深色×3 / 浅色×3 / 跟随系统）、自定义下载保存目录
- 文件管理：上传 / 下载并打开 / 重命名 / 删除、传输任务中心（实时进度）
- 磁盘状态分色显示：运行中绿 / 已休眠黄 / 未挂载灰 / 异常红

### 优化

- 首页加载提速：多个数据查询改为并行请求
- 磁盘明细并入"存储空间"卡片，页面更紧凑
- 数据刷新间隔统一为 2 秒（仪表盘 / Docker / 虚拟机 / 文件管理）
